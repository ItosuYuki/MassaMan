# DESIGN.md — マッサマン (Massage Manager)

> [design-md](https://dev.classmethod.jp/articles/design-md-ai-agent-design-system/) /
> [awesome-design-md](https://github.com/VoltAgent/awesome-design-md) 形式に基づく、
> マッサマンのデザインシステム定義ファイル。`design/*.html`（PC版・スマホ版の画面設計、
> [公開Artifact](https://claude.ai/code/artifact/6a32b806-4bb4-4c34-93e9-ecbca5c38cbf)）
> から抽出した実際のトークン・コンポーネント規則を記載する。以後この製品のUIを実装・
> 拡張するエージェント／開発者は、色・書体・余白を都度指定する代わりに本ファイルを参照する。

## 1. Visual Theme & Mood — ビジュアルテーマと雰囲気

- **世界観**: 社内向けマッサージ室予約システム。派手な演出やマーケティング的な作り込みは
  行わず、情報密度の高い実務ツールとして設計する（一般消費者向けLPではない）。
- **トーン**: 落ち着いた寒色系（青）を基調とした、静けさ・信頼感のあるトーン。旧ブランド
  (「ほぐし予約」) の暖色（緑・生成り）から、マッサマンへの改名時に寒色系へ全面移行した。
- **平面性**: ドロップシャドウ・グラデーションは使用しない、意図的にフラットなスタイル。
  要素の分離は `1px solid var(--border)` の罫線と背景コントラストのみで行う。
- **ブランドモチーフ**: 人魚（プロジェクトの旧コードネーム「Ningyo」に由来）。ロゴマークは
  他の全アイコンと異なり、線画アイコンではなく塗りのイラストシルエットを採用する（後述）。

## 2. Color Palette & Roles — カラーパレットと役割

### 2.1 ベーストークン（全画面共通）

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#EEF3F9` | ページ背景（寒色寄りの明るいグレーブルー） |
| `--surface` | `#FFFFFF` | パネル・カードの地色 |
| `--surface-2` | `#E2E9F2` | 補助的な地色（非アクティブタブ、予約済みグリッドセル等） |
| `--border` | `#CBD7E3` | 罫線 |
| `--ink` | `#1B2733` | 本文・見出しの主色 |
| `--ink-soft` | `#4C5C6E` | 二次テキスト |
| `--ink-faint` | `#7C8CA0` | 三次テキスト・プレースホルダー |
| `--accent` | `#2B5C99` | プライマリブルー（ボタン・リンク・主要アクション） |
| `--accent-strong` | `#1D4370` | 濃いブルー（強調見出し、ヒーローパネル背景） |
| `--accent-soft` | `#DCE7F5` | 薄いブルー（バッジ、淡いハイライト） |

### 2.2 ロール別ティント（利用者／マッサージ師／管理者）

同じ「青系」ファミリーの中で、明度・彩度だけを変えて3つのロールを描き分ける
（色相を変えて別ブランドのように見せない）。

| ロール | メイン | ソフト | 用途 |
|---|---|---|---|
| 利用者 | `--role-user` `#2E74B8` | `--role-user-soft` `#DCEBF7` | 明るい青。利用者向け画面のアクティブナビ等 |
| マッサージ師 | `--role-therapist` `#2F5E86` | `--role-therapist-soft` `#DDE7F0` | 中間のスレートブルー |
| 管理者 | `--role-admin` `#1B3A5C` | `--role-admin-soft` `#DCE1EA` | 最も濃いネイビー（権限の重さを表現） |

### 2.3 セマンティックカラー（ブランドカラーとは独立）

意味を持つ色はブランドの青系に置き換えない。テーマが変わっても固定。

| 用途 | 値 | 例 |
|---|---|---|
| 警告・自動選択バッジ | `--amber` `#9C6A1E` / `--amber-soft` `#F2E4C8` | 「自動選択（負荷分散を考慮）」ピル |
| 評価（星） | `--star` `#C9922E` | 口コミの★表示 |
| 破壊的操作 | `#C4523A`（直書き、トークン化していない） | キャンセルボタンの縁取り・文字色、現在時刻ライン |

破壊的操作は**塗りつぶしの赤ボタンにはしない**。白地＋赤の縁取り／文字色に留め、UI全体の
落ち着いたトーンを崩さない。

## 3. Typography Rules — タイポグラフィ規則

3書体をGoogle Fontsから読み込む。各画面ファイルは独立ドキュメントのため、
以下の`@import`を画面ごとに個別に持つ（共通スタイルシートは無い）。

```css
@import url('https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@500;700&family=Noto+Sans+JP:wght@400;500;700;900&family=JetBrains+Mono:wght@500;700&display=swap');
```

| 書体 | ウェイト | 用途 |
|---|---|---|
| Shippori Mincho（明朝） | 500 / 700 | `h1`〜`h3` 見出し全般。ロゴ副題「Massage Manager」（`Massa`/`Man`のみ太字にして語呂を視覚化） |
| Noto Sans JP（ゴシック） | 400 / 500 / 700 / **900** | 本文・UIラベル全般。900は「マッサマン」ロゴタイプ専用（太いゴシック、`letter-spacing:-0.02em`） |
| JetBrains Mono（等幅） | 500 / 700 | `.mono`ユーティリティ。時刻・日付・件数・パーセンテージなど**数字を含む値は必ずこれを使う**。`font-variant-numeric: tabular-nums`とセットで桁を揃える |

## 4. Component Styles — コンポーネントスタイル

### 4.1 ロゴ／アイコン（唯一の例外）

- **ブランドマーク**: 人魚のイラストを実画像からトレースした塗りシルエット
  （PNG、`<img>`埋め込み）。他の全アイコンが採用する「線画・24×24 viewBox」の
  規則には**あえて従わない**唯一の例外。青版（`#1D4370`系）と白版（暗い背景用）の
  2バリアントを持つ。
- **バッジ**: 角丸9〜20pxの正方形コンテナ。背景はロール別ソフトカラー
  （利用者/マッサージ師/管理者ページはそれぞれ`--role-*-soft`、ログイン画面の
  暗いヒーローパネル上は`rgba(255,255,255,.15)`）。現在は`background.jpg`を
  テクスチャとして `background: <色> url(...) center/cover no-repeat` で重ねている。
- **ワードマーク**: アイコン右に2行ロックアップ。1行目「マッサマン」
  （Noto Sans JP 900）、2行目「Massage Manager」（Shippori Mincho、`Massa`/`Man`太字）。
- **その他の全アイコン**: インラインSVG、`viewBox="0 0 24 24"`、
  `fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"`。
  色は`currentColor`で親のテキスト色を継承する（ナビのアクティブ/非アクティブで自動的に色替え）。

### 4.2 コンテナ

- **`.panel`**: 最も基本的なカード。`background:var(--surface); border:1px solid var(--border); border-radius:16px; padding:20px 22px;` グループ化されたコンテンツ（フォーム、統計、リスト）は必ずこれで囲む。

### 4.3 ボタン

| 種別 | スタイル |
|---|---|
| プライマリ | `var(--accent)`塗り、白文字、角丸12〜14px、高さ44〜52px（モバイルの固定フッターCTAは52px） |
| セカンダリ／ゴースト | 白背景＋`var(--border)`の縁取り、`var(--ink-soft)`文字 |
| 破壊的 | 白背景＋`#C4523A`の縁取り・文字（塗りつぶしにしない） |

### 4.4 選択系コントロール

- **`.chip` / `.daychip` / `.duration-chip` / `.viewtab`**: 小型のピル/セグメント。
  未選択は`--border`枠か`--surface-2`地、選択(`.picked`/`.active`)は
  `var(--accent)`塗り＋白文字（タブ型は`--surface-2`地＋太字inkの場合もある）。
- **チェックボックス**: ネイティブ`<input type="checkbox">`（14px、独自スキン無し）。
  性別絞り込みなどの補助条件は**常にデフォルト表示**し、トグルの裏に隠さない。
- **スライダー**: 高さ6pxのフラットトラック（`--surface-2`）＋`var(--accent)`の
  塗りバー＋白丸＋アクセント縁のつまみ。5分刻みなど段階のあるスライダーは
  トラック上に小さいドットの目盛りを追加する。

### 4.5 施術者選択

- **`.therapist-row` / `.therapist-auto-card`**: 円形アバター（頭文字モノグラム、
  ロールタイントの円背景）＋名前＋得意分野のサブテキスト。選択中は`.picked`
  （アクセント枠＋`accent-soft`地）。自動選択されたものには`.pill-auto`
  （amber-soft地／amber文字の「自動選択（負荷分散を考慮）」バッジ）を添える。
  性別チェックボックスと施術者リストは**同一パネル内に常時表示**し、
  「おまかせ/指定する」のようなモード切り替えで隠さない。

### 4.6 空き状況表示

- 記号方式：`✓`（予約済み・確定枠）／`◯`（空き）／`✕`（埋まってる）。
  テキストピル（「空き」「予約済み」等）ではなく、この3記号を各セル／スロットに
  直接表示する。グリッド・リストいずれの上部にも凡例行（記号＋ラベル）を添える。

### 4.7 トグルスイッチ

- 通知ON/OFFなど：38×22pxのピル形トラック。ONは`var(--role-*)`塗り、OFFは
  `--surface-2`地＋枠線。白い丸ノブが左右にスライドする。

## 5. Layout Principles — レイアウト原則

- **PC版**: 1280×800の固定キャンバス。左に220px固定サイドバー
  （ロゴ+ワードマーク／ナビ／ロール別フッター）＋残り幅のメインカラム
  （パディング26〜32px）。
- **スマホ版**: 390×844の固定キャンバス（iPhoneサイズ）。上部にアプリバー
  （戻り山括弧＋タイトル＋サブタイトル）、スクロール可能な本文、画面下部に
  フェードグラデーション付きの「sticky」CTAボタン。
- **余白**: パネル間の gap は14〜18px、ページ全体のパディングは20〜32px。
- **ナビのアクティブ状態**: サイドバーの選択中ナビ項目は、そのロールの
  `-soft`地＋`-role`色文字＋`font-weight:500`に切り替わる。
- **1ルート=1コンポーネント**: モバイル/PC版が別々の`.dc.html`として存在するのは
  静的モックアップだからであり、実装（Next.js）では**1つのルートに1ページ**、
  Tailwindのブレークポイントで両レイアウトを1コンポーネントにまとめる
  （`docs/superpowers/specs/2026-09-08-nextjs-scaffold-design.md`参照）。

## 6. Depth and Elevation — 深さと段階

- **完全にフラット**。ドロップシャドウは一切使用しない。要素の分離は
  「1px罫線＋背景コントラスト」のみで表現する。
- モーダル（口コミ投稿ダイアログ）は`box-shadow`とスクリムのオーバーレイを
  使う設計だったが、当該機能自体を撤去したため、現行画面にモーダルは存在しない。
  将来モーダルを追加する場合も、シャドウは最小限（`0 12px 32px rgba(20,30,20,.25)`
  程度）に留め、フラットな基調を崩さないこと。

## 7. Do's and Don'ts

**Do**
- ブランドの「顔」となる色は青系ファミリー（`--accent` / `--role-*`）だけに限定する。
- 性別チェックボックス・施術者リストのような主要な意思決定コントロールは、
  常にデフォルト表示にする。
- 数字を含む値（時刻・件数・％）は必ず`.mono`（JetBrains Mono）で表示する。
- 人魚のロゴマークは実画像トレースの塗りシルエットのまま使う。

**Don't**
- 警告色（amber）・評価色（star gold）・エラー色（`#C4523A`）をブランドカラーで
  置き換えない。意味を持つ色は常に固定。
- ロゴマークを他のアイコンと同じ「線画・24×24」規則に合わせて描き直さない
  （意図的な例外として扱う）。
- ドロップシャドウ・グラデーションを新規に追加しない。
- 主要な選択操作をトグルの裏に隠さない（例：施術者を「おまかせ/指定する」の
  トグルで分岐させ、片方を非表示にするような設計は不採用となった）。
- 消費者向けLPのような大きなヒーロー演出・キャッチコピーを追加しない
  （社内実務ツールとしてのトーンを保つ）。

## 8. Responsive Behavior — レスポンシブ動作

現行の`design/`はスマホ版・PC版を別ファイルの静的モックアップとして管理しているが、
実装時のレスポンシブ方針は以下の通り：

- Tailwindのブレークポイント（`sm:` `lg:`等）で1コンポーネント内にモバイル/PC
  レイアウトを共存させる。別ルート・別ページには分けない。
- モバイルは1カラム＋下部固定CTA、PCはサイドバー＋複数カラムのグリッドに
  切り替わる。
- 詳細は `docs/superpowers/specs/2026-09-08-nextjs-scaffold-design.md` の
  「モバイル vs. 予約desktop」節を参照。

## 9. Agent Prompting Guide — エージェント向けプロンプトガイド

このリポジトリでUIを実装・拡張するエージョンへの指示：

1. **色を聞かれたら**まず本ファイルの §2 のトークン表を引用する。新しい色を
   都度提案しない。
2. **書体を聞かれたら** §3 の3書体（Shippori Mincho / Noto Sans JP / JetBrains Mono）
   以外を追加しない。数字は必ずJetBrains Monoで統一する。
3. **アイコンを実装するとき**、ロゴマーク以外は §4.1 の線画SVG規則
   （24×24 viewBox、stroke 1.8、currentColor）に必ず従う。ロゴマークだけは
   例外であることを毎回思い出すこと（線画化して「揃えよう」としない）。
4. **新しい画面を作るとき**は、まず `design/*.html`（PC版・スマホ版）の中から
   一番近いレイアウトのファイルを探し、その構造（サイドバー幅・パディング・
   パネルの入れ子）を踏襲する。ゼロから余白やグリッドを設計し直さない。
5. **意思決定の背景を確認したいとき**は、GitHub Issue（`ItosuYuki/MassaMan`
   旧`Ningyo`リポジトリ）のクローズ済みIssue #1〜#4、および
   `docs/superpowers/specs/`配下の設計ドキュメントを参照する。
6. **迷ったら平面的（フラット）・寒色系（青）・実務ツールらしい落ち着いたトーン**
   を優先する。派手さ・装飾を足す方向には倒さない。
